## 1. 新建策略单页与默认值

- [x] 1.1 移除或合并新建态下的 Tabs：在同一可视页面内分组展示「名称与执行绑定」「文件备份配置」「可选备份计划（单条）」；使用 Card、`Row`/`Col` 控制密度与并排布局，保证小屏可滚动、字段分组清晰。
- [x] 1.2 新建表单初始将 `encrypt_artifacts`（加密上传制品）设为默认开启；加载已有策略编辑态时仍以服务端数据为准。
- [x] 1.3 新建页增加「可选一条备份计划」表单区块（cron、timezone、enabled 等与现有新建计划模态对齐的最小字段集）；未填写任一必填计划字段则跳过计划创建。
- [x] 1.4 实现保存编排：先创建策略，再按需创建一条 Schedule；错误处理与用户提示按 `design.md` 决策 1。

## 2. 策略列表：启用 Switch 与确认

- [x] 2.1 在 `ProTable` **操作列**内放置受控 `Switch`（或滑钮），绑定当前行 `enabled`（不设独立「启用」列）。
- [x] 2.2 切换前弹出二次确认 Modal；启用与禁用使用区分文案；取消则不改状态。
- [x] 2.3 确认后调用更新策略接口；成功后 `actionRef.reload()`；失败 `message.error` 并保持原状态。

## 3. 编辑策略：右侧抽屉与只读规则

- [x] 3.1 将编辑入口改为打开右侧 Drawer（推荐由列表页承载）；新建仍可走 `/backup/policies/new` 全页或按产品统一入口（与列表「新建」按钮一致即可）。
- [x] 3.2 编辑抽屉内复用表单逻辑：路径输入区与 `bound_agent_id` 选择在**所有策略状态下** `disabled` + 置灰；保存 payload 不修改这两项（或固定传原值）。
- [x] 3.3 保存成功后：刷新策略列表；**不关闭** Drawer；表单可与服务端再次对齐（可选 `GET` 单策略）。

## 4. 国际化与无障碍

- [x] 4.1 补充 `zh-CN` / `en-US`：启用/禁用确认标题与正文、只读字段辅助说明（如有）。
- [x] 4.2 Switch + Modal 焦点与键盘操作合理（Ant Design 默认行为一般可满足）。

## 5. 验证

- [x] 5.1 手动验证：新建仅策略、新建策略+一条计划、计划创建失败时的提示与列表状态。
- [x] 5.2 手动验证：列表启用/禁用确认与刷新；无写权限用户不展示 Switch 或与 `access.canWrite` 一致。
- [x] 5.3 手动验证：编辑抽屉路径与 Agent 不可编辑；其它字段可保存；保存后列表刷新且抽屉仍打开。

## 6. 提案修订（启停入口、布局与列表列）

- [x] 6.1 **新建页与编辑抽屉**：移除策略级「启用」表单项；新建 `POST` **固定** `enabled: true`；编辑 `PATCH` **不传** `enabled`（除非产品改为抽屉内可启停）。
- [x] 6.2 **紧凑并排布局**：新建页与 `PolicyFormFields`（及抽屉内）使用 `Row`/`Col` 或 Form 栅格，使相关字段在中等宽度以上可并排，减少右侧留白；路径/排除等大字段可独占整行。
- [x] 6.3 **策略列表**：将启用 **Switch 移入「操作」列**，删除或合并原独立「启用」列；只读用户仅在操作列或其它紧凑位置展示启用状态（如 Tag），不单独占一列开关。

## 7. 抽屉内备份计划表（启用进操作列、可编辑、无计划管理外链）

- [x] 7.1 **列与操作**：删除计划表的独立「启用」列；在 **操作列** 内放置 **Switch** 控制 `enabled`，行为与策略列表一致（二次确认 + `PATCH` schedule）；保留 **删除**。
- [x] 7.2 **编辑**：操作列增加 **「编辑」**，打开弹窗编辑 **cron、timezone**（`PATCH`）；**启用仅由操作列 Switch 控制**，编辑弹窗不出现与 Switch 重复的启用项。
- [x] 7.3 **移除外链**：去掉行内「计划管理」/跳转合规计划总表的 `Link`；文案键若仅用于该链可清理或复用。
- [x] 7.4 **国际化**：补充计划启用确认、编辑弹窗标题等 `zh-CN` / `en-US`（可与策略列表复用模式或独立 key）。

## 8. Agent 主机名呈现与执行面列表

- [x] 8.1 **策略列表**：「绑定执行组件」列改名为 **Agent 主机**；单元格展示 **主机名**（由 `bound_agent_id` + 租户 Agent 列表解析）；链接/跳转行为改为不以 ID 为可见主文案（若保留详情入口，用主机名或抽屉）。
- [x] 8.2 **新建/编辑策略**：`PolicyFormFields` 标签改为 **Agent 主机**；`Select` 仅展示 **主机名**；移除 **「租户内执行组件」** 外链。
- [x] 8.3 **租户内执行组件页**：移除 **执行组件 ID** 列；**主机名**可点击；点击打开 **右侧 Drawer** 展示组件快照/详情（复用或抽取 fleet detail 数据请求）。
- [x] 8.4 **全舰队页**：同上（去 ID 列、主机名进抽屉）。
- [x] 8.5 **国际化**：`zh-CN` / `en-US` 更新列名、标签、回退文案（如未上报主机名）。

## 9. 作业中心列表与策略列表列顺序

- [x] 9.1 **作业中心表格**：移除 **租约主机**列；**完成主机**列标题改为 **Agent 主机**；移除 **错误**列。
- [x] 9.2 **作业详情抽屉**：展示 **错误信息**（`error_message` 等）；列表不展示租约列时抽屉内不单独展示租约主机行（与 design 一致）。
- [x] 9.3 **策略列表**：将 **创建时间**列移到 **第一列**（先于名称）。
- [x] 9.4 **国际化**：`page.jobs` 相关列名/抽屉标签 `zh-CN` / `en-US`。

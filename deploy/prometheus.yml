# DeVault local stack: scrape the control-plane HTTP /metrics endpoint.
# See deploy/docker-compose.yml service `prometheus`.

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: devault-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - api:8000
